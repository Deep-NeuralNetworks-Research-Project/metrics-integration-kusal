# paper/ — P5 integration owner

- `main.tex` and `references.bib` are owned by P5. Do not alphabetise away unused keys until a pass that also greps `\cite`.
- One file per section in `sections/` so P1–P4 can PR without fighting `main.tex`.
- Key convention: `author_year_firstword`.
- Overleaf: ACM `acmart` `[sigconf]`. Add all five as collaborators; GitHub sync this folder.
- Weekly review (weeks 8–13): P5 reads every section, not just at the end. Tick in the project board.
- Figures: see `docs/figure_standards.md`. Vector PDF, Okabe-Ito overlays, no red-green.
