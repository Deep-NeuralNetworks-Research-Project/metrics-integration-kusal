\section{Results}
\label{sec:results}

% Tables are generated by cdlib.metrics.stats.ablation_table.
% Do not paste a number that lacks π, pooling protocol, and threshold.

\subsection{Overlap}
\emph{TBD --- waiting on $\ge 3$-seed runs.} Report aggregate F1/IoU
with $\pi$, plus mF1. State the validation-only threshold.

\subsection{Boundary and false alerts}
\emph{TBD.} Headline boundary number is boundary F1 at 2\,px
(Csurka et al., BMVC 2013). Boundary IoU, HD95, and false-alert rate
vs.\ $k$ are secondary.

\subsection{Change size}
\emph{TBD.} F1 and SCE$_{\mathrm{flip}}$ stratified by ground-truth
component area (provisional bins until the LEVIR histogram lands).

\subsection{Robustness}
\emph{TBD.} Retention heatmap, condition $\times$ severity $\times$
direction. Asymmetry between T1-corrupted and T2-corrupted is a
finding, not a bug.

\subsection{Calibration and deferral}
\emph{TBD.} Class-wise ECE before/after temperature scaling on the
shift slice; reliability diagrams; risk--coverage with SDC vs.\ MSR.

\subsection{Efficiency}
\emph{TBD.} One row per architecture (not per seed). GPU model, AMP,
TF32, batch size, FLOP tool and convention disclosed.
